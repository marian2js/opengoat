# OpenCode Provider

- Type: CLI
- Command: `opencode`
- Env override: `OPENCODE_CMD`
- Optional default model: `OPENCODE_MODEL`
- External agent creation: supported via agent files in `~/.config/opencode/agent` (or `OPENCODE_CONFIG_DIR`)
