{
  "name": "@opengoat/openclaw-plugin-pack",
  "version": "0.1.0",
  "private": true,
  "description": "OpenGoat monorepo workspace",
  "type": "module",
  "packageManager": "pnpm@10.18.2",
  "scripts": {
    "build": "pnpm --filter @opengoat/core build && pnpm --filter @opengoat/ui build && pnpm --filter opengoat build",
    "clean": "pnpm --filter @opengoat/core clean && pnpm --filter @opengoat/ui clean && pnpm --filter opengoat clean && rm -rf coverage",
    "dev": "pnpm --filter opengoat dev",
    "start": "pnpm --filter opengoat start",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "pnpm --filter @opengoat/core typecheck && pnpm --filter opengoat typecheck",
    "changeset": "changeset",
    "version-packages": "node scripts/release.mjs",
    "release": "node scripts/release.mjs"
  },
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  },
  "files": [
    "index.ts",
    "openclaw.plugin.json",
    "packages/openclaw-plugin/index.ts",
    "packages/openclaw-plugin/openclaw.plugin.json",
    "packages/openclaw-plugin/src/**/*.ts",
    "!packages/openclaw-plugin/src/**/*.test.ts",
    "packages/core/src/**/*.ts",
    "!packages/core/src/**/*.test.ts",
    "packages/core/src/core/templates/assets/**/*.md"
  ],
  "engines": {
    "node": ">=20.11.0"
  },
  "pnpm": {
    "overrides": {
      "lodash-es": ">=4.17.23",
      "@trpc/client": "10.45.4",
      "@trpc/server": "10.45.4"
    },
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.8",
    "@types/node": "^22.13.4",
    "typescript": "^5.9.3",
    "vitest": "^4.0.5"
  }
}
